PLUSMENU VERIFICATION RESULTS:

MOBILE: FIXED - Bottom sheet shows all items clearly:
- MEDIA: Add files, Share screen, Record video, Upload video
- CREATE: Build website, Create slides (Pro), Create image, Edit image
- Scrollable to see more items (Create spreadsheet, Create video, Generate audio, TOOLS, CONNECTORS)
- All text fully readable, no clipping

DESKTOP: FIXED - Popover shows MEDIA section at top, scrollable to see all sections
- Menu is constrained within viewport (top: 8px clamp)
- Scrollable to access all items
- No items clipped off-screen

Now need to fix remaining issues:
1. TaskView input bar on mobile - still cramped
2. Pages bottom padding - content cut off by bottom nav
